"""FundOps API route modules.

Each module exposes `router = APIRouter()` and is mounted under `/api` by
`backend.api.create_app()` (see docs/api-contract.md "Route module ownership").
This package intentionally has no import-time side effects.
"""
