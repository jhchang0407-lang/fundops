# Spec-First Behavior Changes

FundOps behavior changes will be defined through domain context, ADRs, a reviewed PRD, and eval/acceptance criteria before application code is changed. We chose this because the workflow has many overlapping surfaces, and implementation-first changes can accidentally collapse distinct product concepts or create duplicate behavior before the intended boundaries are agreed.
